name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Deployment boundary
        run: echo "Deploys live in this workflow once hosting targets are connected."
